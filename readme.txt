svm-final.ipynb is the jupyter notebook of our project model
app.py is the ui code
test & train is the dataset

To run the project:
Type "streamlit run app.py" in the command prompt with the working directory opened